{
  "name": "workbench-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "清晨工作台 - Capacitor Android 包装",
  "scripts": {
    "sync": "cap sync android",
    "build:android": "cap sync android && cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}